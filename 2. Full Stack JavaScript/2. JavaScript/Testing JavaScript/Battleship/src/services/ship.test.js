/*
- Ship class exist if it returns an object of type Ship
- Ship has a length
- Ship takes hits
-- hit() function
-- Hits increase, each time the ship is hit
-- Hits cannot exceed the ship length
- Ship is sunk
-- isSunk() function
-- if the number of hits < length, the ship is not sunk
-- if the number of hits = lenght, the ship is sunk
*/
